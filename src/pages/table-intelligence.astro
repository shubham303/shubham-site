---
// The platform landing — always reachable here, regardless of the PLATFORM_HOME
// flag that controls what the site root (/) shows.
import PlatformHome from '../components/home/PlatformHome.astro';
export const prerender = false;
---
<PlatformHome />
