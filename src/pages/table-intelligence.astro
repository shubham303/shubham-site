---
// The Table Intelligence landing now lives at the site root (/).
// This route is kept as a permanent redirect for existing links/bookmarks.
export const prerender = false;
return Astro.redirect('/', 301);
---
