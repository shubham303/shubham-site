---
import PlatformHome from '../components/home/PlatformHome.astro';
export const prerender = false;

// The site root is the Table Intelligence landing (the hub). Sub-pages
// (About, Blog, Pricing, Docs) are leaves with a brand-only header back to /.
---
<PlatformHome />
