// Env-driven feature flags.
//
// Note: the former `platformHomeEnabled` flag has been removed — the site
// root (/) is now always the Table Intelligence landing. The personal
// portfolio content lives on /about.
